@{
    ViewData["Title"] = "Dashboard";
}

<div class="container mt-4">
    <nav class="navbar navbar-expand-lg navbar-light bg-light rounded mb-4">
        <a class="navbar-brand font-weight-bold" href="@Url.Action("Index", "Dashboard")">🌱 AgricultureEMS</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item"><a class="nav-link" href="@Url.Action("Index", "Crops")">Crops</a></li>
                <li class="nav-item"><a class="nav-link" href="@Url.Action("Index", "Fields")">Fields</a></li>
                <li class="nav-item"><a class="nav-link" href="@Url.Action("Index", "Equipment")">Equipment</a></li>
                <li class="nav-item"><a class="nav-link" href="@Url.Action("Index", "PestReports")">Pest Reports</a></li>
                <li class="nav-item"><a class="nav-link" href="@Url.Action("Index", "Account")">Users</a></li>
            </ul>
        </div>
    </nav>
    <div class="jumbotron">
        <h1 class="display-4">Welcome to AgricultureEMS Dashboard</h1>
        <p class="lead">
            Manage your farm’s crops, fields, equipment, and pest reports all in one place.<br>
            Use the navigation bar above to get started.
        </p>
        <hr class="my-4">
        <div class="row text-center">
            <div class="col-md-3 mb-2">
                <a href="@Url.Action("Index", "Crops")" class="btn btn-success btn-block">Manage Crops</a>
            </div>
            <div class="col-md-3 mb-2">
                <a href="@Url.Action("Index", "Fields")" class="btn btn-info btn-block">Manage Fields</a>
            </div>
            <div class="col-md-3 mb-2">
                <a href="@Url.Action("Index", "Equipment")" class="btn btn-warning btn-block">Manage Equipment</a>
            </div>
            <div class="col-md-3 mb-2">
                <a href="@Url.Action("Index", "PestReports")" class="btn btn-danger btn-block">Report a Pest Issue</a>
            </div>
        </div>
    </div>
</div>